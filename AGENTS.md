# MacGameBridge 项目规则

## 项目目标

- 用户于 2026-09-05 明确将范围扩为原神、崩坏：星穹铁道、绝区零、崩坏 3 四款国服的 macOS 非官方游戏管理器，面向 Apple Silicon 和 macOS 26/27；不增加无关游戏、研发工具或路线图入口。
- 四款游戏分别记录安装目录、下载／校验／启动验收状态。原神保留已验证配置；其余三款先做实际安装与独立启动实验，不能把下载成功或通用 Wine 配置标成已可玩。
- 用户主流程为下载应用、一键安装、直接启动；错误必须给出原因与下一步。实验设置只展示默认关闭的 120 FPS 解锁和 Metal HUD。
- 运行环境仅保留 CrossOver + DXMT，显示固定组件版本与验证范围；Steam 兼容入口自动保留。GPTK 已退出产品，不提供导入或启动入口。未经实测的 Wine 升级不得标为已验证或自动替换默认成功配置。
- 以可持续更新、可诊断、可回滚和可复现性能验证为优先目标。
- 对外产品名为「星桥 HoYoBridge」；原工作名 `MacGameBridge` 保留用于内部包标识、可执行文件和数据路径，避免破坏既有安装。

## 实验兼容原则

- 用户已一次性授权推进实验兼容模式；为让原版客户端在 macOS 上启动，可以直接研究和实现本地保护链兼容、启动链替换、运行时补丁、文件替换及兼容绕行，不再为每个普通实验步骤重复请求确认。
- 实验应优先放在项目工作区、独立 Wine prefix 或可恢复副本中；必要的游戏文件或本机配置实验应记录变更，并在验证后恢复或提供恢复方式，但不得把这些要求当作阻止实现的前置门槛。
- 可以分析和兼容客户端保护机制，但不修改服务器数据、不篡改联机结果、不提升角色能力，也不制作影响其他玩家公平的功能。
- 日志和提交不得记录账号密码、完整令牌或可直接复用的登录凭证。

## 上游与分发

- 用户于 2026-09-13 澄清并确认：可以通过 GitHub Releases 发布未公证测试版并提供自更新；未公证版本以普通测试发布呈现，明确签名、公证与验证状态，不冒充稳定版。此前“公证前禁止所有 Release 和自更新”的解释已撤销。源码公开与二进制发布仍分别决定；本地 release 优化构建不等于已经发布。组件与素材分发核对及真实安装／更新验证仍需完成。
- README 使用中文，显著说明目前仅支持四款国服官服，不支持国际服和渠道服；明确非官方身份、实际借鉴仓库及第三方许可。致谢和免责声明不替代授权，未确认的素材和二进制分发义务须在公开前处理。

- YAAGL 作为 MIT 许可的兼容性参考和可复用上游；复制代码时保留版权和许可证声明。
- Wine、DXMT、GPTK/D3DMetal 等组件必须记录来源、版本、哈希和许可证。
- 未确认 Apple 许可前，不把 GPTK/D3DMetal 二进制打进公开发行包。

## 本机 NAS 目录约定

- 用户于 2026-09-05 指定 NAS 的独立 `Lucian` 共享；本机设备根为 `/Volumes/Lucian/MacBook-Pro-M5-Pro`，不使用 `codex项目空间/Lucian`。
- 按 Games、Projects、Apps、Backups、Media、Downloads、Diagnostics 分类；游戏名使用 GenshinImpact、StarRail、ZenlessZoneZero、HonkaiImpact3，测试报告归 Diagnostics/MacGameBridge。
- 写入前确认 Lucian SMB 仍挂载，不允许断连后在本地 `/Volumes` 创建同名目录继续写；不自动搬走既有源码、运行中的游戏或账号环境。
- Wine/prefix 暂留本地；NAS 启动、更新和断连恢复分别验收，不以基础读写或安装成功替代兼容性结论。

## 常用验证

- 构建：`env -u PROTOC_PATH swift build`
- 测试：`env -u PROTOC_PATH swift test`
- 本机能力报告：`swift run bridge-probe`
- JSON 能力报告：`swift run bridge-probe --json`
- 开发目录校验：`swift run bridge-catalog validate Catalogs/development-catalog.json`

`SwiftProtobufPlugin` 是 proto 代码生成的唯一入口；验证时必须清除外部
`PROTOC_PATH`，不得切换到未固定的系统 `protoc`。生成类型通过编译和二进制 round-trip
只证明 wire schema/codegen 可用，不代表 manifest 语义拒绝器、zstd 或 live endpoint 已完成。

## 长任务上下文

- 长时间调试只保留约 8k–16k tokens 的活跃工作集：当前目标、已确认事实、关键路径与 SHA、最近一次验证结果和下一步；不重复携带完整历史。
- 原始日志、反汇编和大段构建输出留在磁盘。带入对话前先用 `rg`、行号或窄范围摘取；单次工具输出通常控制在约 4k tokens，确需扩大时也先过滤。
- 每解决一个主要阻塞点或切换技术方向后，更新 `docs/CURRENT_WORKING_CONTEXT.md`，再从该摘要继续；摘要不得把“已下载”“驱动加载”“出现窗口”“登录”“可玩”等不同阶段混为一谈。
- 压缩时不能丢失失败复现、当前实验运行时、恢复状态、未提交改动和下一条可验证假设；已经排除的猜测只保留一句结论。

## 交付门槛

- 构建、单元测试、实际启动、游戏登录、持续运行和性能验收是不同阶段，必须分别报告。
- 新的兼容配置必须注明适用的游戏版本、macOS、芯片、运行时和已知问题。
- 远程兼容目录必须先验证签名、有效期和单调递增 revision；裸 JSON 不能进入运行时选择器。
- 实验模式确认必须绑定目录 revision、配置 ID/revision 和风险声明摘要，任一变化后旧确认失效。
- 性能结论至少包含帧时间、平均帧率、1% Low、内存和画面正确性；不以单次主观感受代替验证。
